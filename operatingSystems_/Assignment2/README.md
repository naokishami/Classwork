For this assignment, we built a program that uses multithreading and mutex locks to compute the average of numbers.

To compile:
```
make
```

To execute:
```
./main
```

To clean:
```
make clean
```
